#ifndef MENUTYPE_H
#define MENUTYPE_H


//All the different menu type

// normalMenu = menu with the difficulties
// startMenu = main menu
// breakMenu = menu when its the break
// gameOverMenu = menu if the player loses
// victoryMenu = menu if the player wins
enum MenuType {
    normalMenuType,
    startMenuType,
    breakMenuType,
    gameOverMenuType,
    victoryMenuType
};

#endif // GAMESTATE_H

